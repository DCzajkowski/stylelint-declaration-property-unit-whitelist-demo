# Demo of stylelint-declaration-property-unit-whitelist-demo

## Installation

```bash
# clone the repository, run `yarn` and `yarn lint`
yarn
yarn lint # This should show several errors proving the rule works as expected
```
